# Quiz Education - Профорієнтаційне тестування

Сучасна платформа для профорієнтаційного тестування з Laravel backend.

## 🚀 Швидке встановлення

```bash
cd backend
composer install
copy .env.example .env
php artisan key:generate
php artisan migrate
php artisan serve
```

Детальна інструкція: [SETUP.md](SETUP.md)

## 📁 Структура проекту

```
quiz/
├── backend/                      # Laravel Backend
│   ├── app/
│   │   ├── Http/Controllers/
│   │   │   ├── AuthController.php
│   │   │   ├── QuizController.php
│   │   │   ├── AdminController.php
│   │   │   └── PaymentController.php
│   │   ├── Models/
│   │   │   ├── User.php
│   │   │   ├── QuizSession.php
│   │   │   ├── QuizAnswer.php
│   │   │   ├── QuizResult.php
│   │   │   └── Payment.php
│   │   └── Mail/
│   ├── database/migrations/      # 11 міграцій БД
│   ├── resources/
│   │   ├── views/reports/        # Blade шаблони звітів
│   │   └── ai_templates/         # interpretation.json
│   ├── routes/
│   │   ├── api.php              # API роути
│   │   ├── web.php              # Web роути (admin)
│   │   └── auth.php             # Auth роути
│   └── public/                   # Статичні файли
├── SETUP.md                      # Інструкція встановлення
├── INSTALL.md                    # Детальна установка
├── IMPLEMENTATION_GUIDE.md       # Гайд по модулях
└── README.md                     # Цей файл
```

## 🎯 Основні можливості

### Тестування
- 8 модулів професійного тестування
- Генерація PDF звітів
- Психологічна карта особистості
- Holland RIASEC типологія
- Типи інтелекту Гарднера

### Адміністрування
- Панель адміністратора
- Управління користувачами
- Контроль доступу до тестів
- Статистика системи

### Платежі
- Інтеграція Fondy
- Автоматична активація доступу
- Email повідомлення

## 🛠 Backend (Laravel)

### Технології
- Laravel 10.x
- Laravel Sanctum (API аутентифікація)
- MySQL/PostgreSQL
- PHP 8.1+
- DomPDF (генерація PDF)

### API Endpoints

**Публічні:**
- `POST /api/register` - Реєстрація
- `POST /api/login` - Вхід
- `POST /api/password/reset` - Скидання паролю

**Захищені (потрібен Bearer token):**
- `GET /api/user` - Дані користувача
- `POST /api/logout` - Вихід
- `GET /api/quiz/modules` - Модулі тестування
- `POST /api/quiz/start` - Старт тесту
- `POST /api/quiz/answer` - Відповідь на питання
- `POST /api/quiz/complete` - Завершення тесту
- `GET /api/quiz/report/{session}` - PDF звіт
- `POST /api/payments/create` - Створити платіж

**Адмін (потрібен is_admin):**
- `GET /admin/dashboard` - Панель адміністратора
- `GET /admin/users` - Список користувачів
- `PUT /admin/users/{id}` - Оновлення користувача
- `DELETE /admin/users/{id}` - Видалення користувача

## 🚀 Швидкий старт

### 1. Встановлення Backend

```bash
# Перейдіть в папку backend
cd backend

# Встановіть залежності (потрібен Composer)
composer install

# Скопіюйте .env файл
copy .env.example .env

# Згенеруйте ключ додатку
php artisan key:generate

# Налаштуйте базу даних в .env файлі
# Потім запустіть міграції
php artisan migrate

# Запустіть сервер
php artisan serve
```

Backend буде доступний за адресою: `http://localhost:8000`

### 2. Створення адміністратора

```bash
php grant-admin.php admin@example.com
```

```bash
# С помощью Python
python -m http.server 5500

# Или через VS Code Live Server
# Нажмите правой кнопкой на index.html -> "Open with Live Server"
```

Frontend будет доступен по адресу: `http://localhost:5500`

## Настройка

### API URL

По умолчанию в `auth.js` используется:
```javascript
const API_BASE_URL = 'http://localhost:8000/api';
```

Измените этот URL, если ваш backend запущен на другом адресе.

### База данных

Создайте базу данных и настройте подключение в `backend/.env`:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=quiz_education
DB_USERNAME=root
DB_PASSWORD=ваш_пароль
```

## Безопасность

✅ **Реализовано:**
- Хеширование паролей (bcrypt)
- API токены (Laravel Sanctum)
- Валидация данных на backend
- CORS настроен
- Защищенные маршруты

## Использование

1. Запустите Laravel backend (`php artisan serve`)
2. Откройте `index.html` в браузере
3. Нажмите "Зарегистрироваться"
4. Заполните форму регистрации
5. Войдите с вашими учетными данными
6. Вы будете перенаправлены на домашнюю страницу

## Дизайн

- 🎨 Современный градиентный фон
- 📱 Полностью адаптивный дизайн
- ✨ Плавные анимации и переходы
- 💬 Красивые уведомления об ошибках/успехе
- 🔄 Анимация загрузки на кнопках

## Требования

- PHP >= 8.1
- Composer
- MySQL или PostgreSQL
- Веб-сервер (для production)

## Дополнительная информация

Подробная документация по backend находится в `backend/README.md`

## Лицензия

Open Source
