# Quiz Education - Профориентационное тестирование

Современная платформа для профориентационного тестирования с Laravel backend и адаптивным дизайном.

## 🚀 Быстрая установка

```bash
cd backend
composer install
copy .env.example .env
php artisan key:generate
php artisan migrate
php artisan serve
```

Подробная инструкция: [SETUP.md](SETUP.md)

## Структура проекта

```
quiz.education-design.com.ua/
├── backend/              # Laravel API backend
│   ├── app/
│   │   ├── Http/Controllers/Api/
│   │   └── Models/
│   ├── database/migrations/
│   ├── routes/
│   └── config/
├── index.html           # Страница входа
├── register.html        # Страница регистрации
├── home.html            # Домашняя страница
├── styles.css           # Стили
└── auth.js              # JavaScript для работы с API
```

## Frontend

### Файлы
- `index.html` - Страница входа в систему
- `register.html` - Страница регистрации
- `home.html` - Домашняя страница после входа
- `styles.css` - Адаптивные стили
- `auth.js` - JavaScript для взаимодействия с Laravel API

### Функции
- Регистрация с валидацией
- Вход с проверкой токена
- Защищенные страницы
- Автоматическое перенаправление
- Адаптивный дизайн для всех устройств

## Backend (Laravel)

### Технологии
- Laravel 10.x
- Laravel Sanctum (API аутентификация)
- MySQL/PostgreSQL
- PHP 8.1+

### API Endpoints

**Публичные:**
- `POST /api/register` - Регистрация
- `POST /api/login` - Вход

**Защищенные (требуют Bearer token):**
- `GET /api/user` - Данные пользователя
- `POST /api/logout` - Выход

## Быстрый старт

### 1. Установка Backend

```bash
# Перейдите в папку backend
cd backend

# Установите зависимости (требуется Composer)
composer install

# Скопируйте .env файл
copy .env.example .env

# Сгенерируйте ключ приложения
php artisan key:generate

# Настройте базу данных в .env файле
# Затем запустите миграции
php artisan migrate

# Запустите сервер
php artisan serve
```

Backend будет доступен по адресу: `http://localhost:8000`

### 2. Запуск Frontend

Откройте `index.html` в браузере или используйте локальный сервер:

```bash
# С помощью Python
python -m http.server 5500

# Или через VS Code Live Server
# Нажмите правой кнопкой на index.html -> "Open with Live Server"
```

Frontend будет доступен по адресу: `http://localhost:5500`

## Настройка

### API URL

По умолчанию в `auth.js` используется:
```javascript
const API_BASE_URL = 'http://localhost:8000/api';
```

Измените этот URL, если ваш backend запущен на другом адресе.

### База данных

Создайте базу данных и настройте подключение в `backend/.env`:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=quiz_education
DB_USERNAME=root
DB_PASSWORD=ваш_пароль
```

## Безопасность

✅ **Реализовано:**
- Хеширование паролей (bcrypt)
- API токены (Laravel Sanctum)
- Валидация данных на backend
- CORS настроен
- Защищенные маршруты

## Использование

1. Запустите Laravel backend (`php artisan serve`)
2. Откройте `index.html` в браузере
3. Нажмите "Зарегистрироваться"
4. Заполните форму регистрации
5. Войдите с вашими учетными данными
6. Вы будете перенаправлены на домашнюю страницу

## Дизайн

- 🎨 Современный градиентный фон
- 📱 Полностью адаптивный дизайн
- ✨ Плавные анимации и переходы
- 💬 Красивые уведомления об ошибках/успехе
- 🔄 Анимация загрузки на кнопках

## Требования

- PHP >= 8.1
- Composer
- MySQL или PostgreSQL
- Веб-сервер (для production)

## Дополнительная информация

Подробная документация по backend находится в `backend/README.md`

## Лицензия

Open Source
